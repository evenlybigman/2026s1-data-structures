1.  800바이트   배열의 메모리 공간의 크기는 배열의 요소 갯수 * 크기기 때문에 4 * 10 * 20 = 800 따라서 크기는 800바이트이다.


2. 1036번지    float의 크기는 4바이트기 때문에 10번째 요소의 주소는 1000 + (9*4) = 1036이므로 1036번지이다.


3. 2번        각각의 메모리 공간 크기는 (1) 40   (2) 80  (3) 40   (4) 40이므로 가장 큰 배열은 2번이다.


4.
//2의 n제곱 배열 만들기
#include <stdio.h>

int main() {
	int two[10];
	int pow = 1;
	for (int i = 0; i < 10; i++) {
		two[i] = pow;
		pow *= 2;
		printf("2의 %d승은 %d입니다.\n", i,two[i]);
	}
	return 0;
}


5.
//person 구조체 만들기
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <string.h>

struct person {
	char name[10];
	int age;
	float pay;
};

int main() {
	struct person me;
	strcpy(me.name, "김주혁");
	me.age = 23;
	me.pay = 56;
	printf("이름: %s\n나이: %d\n월급: %f", me.name, me.age, me.pay);
	return 0;
}


6.
//Complex 자료형 정의하기
typedef struct complex{
	float real;
	float imaginary;
}Complex;

int main() {
	Complex c1, c2;
	return 0;
}


7.
//복소수 더하는 함수 만들기
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

typedef struct complex {
	float real;
	float imaginary;
}Complex;

Complex complex_add(Complex a, Complex b) {
	Complex result;
	result.real = a.real + b.real;
	result.imaginary = a.imaginary + b.imaginary;
	return result;
}

int main() {
	Complex c1, c2;
	printf("복소수 a의 값을 입력하세요.\n");
	printf("실수의 값:");
	scanf("%f", &c1.real);
	printf("허수의 값:");
	scanf("%f", &c1.imaginary);
	printf("복소수 b의 값을 입력하세요.\n");
	printf("실수의 값:");
	scanf("%f", &c2.real);
	printf("허수의 값:");
	scanf("%f", &c2.imaginary);
	printf("복소수 a + b의 값은 %.2f + %.2fi입니다.",complex_add(c1,c2).real,complex_add(c1,c2).imaginary);
	return 0;
}
